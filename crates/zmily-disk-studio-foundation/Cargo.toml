[package]
name = "zmily-disk-studio-foundation"
version.workspace = true
edition = "2024"
rust-version.workspace = true
description = "Reusable storage and file-format primitives without product workflows or device access"
license = "GPL-3.0-only"
repository = "https://github.com/ByronAP/zmily-disk-studio-foundation"
publish = false

[features]
# Explicitly available to private consumer tests; never enables product code.
test-fixtures = []

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
base64 = { workspace = true }
md-5 = { workspace = true }
sha1 = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
